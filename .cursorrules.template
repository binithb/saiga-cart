# Cursor Rules — {{WORKSPACE_NAME|default("AI Workspace")}}

- Always read `AGENTS.md` and `docs/workflow.md` before generating code or planning changes.
- Architecture contracts are documented in `docs/architecture.md`.
- Sibling repositories are checked out alongside this workspace directory.
- For high-risk or architectural changes, refer to existing ADRs in `docs/adr/`.
- Adhere to the Definition of Ready (DoR) and Definition of Done (DoD) in `docs/workflow.md`.
